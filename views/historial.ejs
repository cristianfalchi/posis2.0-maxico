<%- include('partials/head', {title: "Maxico Srl - Posis 2.0"}) %>

<body>
    <div class="container">
        <!-- menu de navegacion -->
        <%- include('partials/navbar') %>

        <!-- animacion de uploading -->
        <%- include('partials/uploading', {imgUploading: 'uploading.gif'}) %>
        
        <div class="container__info stock animate__animated animate__fadeIn animate__faster">
            <div class = "title">
                <h2>Historial</h2>
            <hr/>
            </div>
            <div class="div-table">
                <table >
                    <thead>
                        <tr >
                          <th>Numero Secuencia</th>
                          <th>Fecha</th>
                          <th>Informado</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for( let i = 0; i < record.length; i++ ) { %>
                        <tr>
                            <td><%= record[i].NumSecuenciaP %> </td>
                            <td><%= record[i].FechaSecuenciaP %></td>
                            <td class=<%= (record[i].Informado === 'NO') && 'no-informado'  %> ><%= record[i].Informado %></td>
                        </tr>
                        <% } %>
                    </tbody>
                  </table>
            </div>
        </div>
    </div>

    <script type="text/javascript" >

        function handleSendData() {
            document.querySelector('.uploading').style.display = "block";
        }
    
        </script>

</body>
</html>